'use client';import {useEffect,useState} from 'react';export default function WhatsAppFab(){const[showAdd,setShowAdd]=useState(false);useEffect(()=>{setShowAdd(false);},[]);const phone=process.env.NEXT_PUBLIC_WHATSAPP_PHONE||'';return(<div className='fixed bottom-6 right-6'>{showAdd?(<button className='btn btn-primary shadow-soft'>Agregar al carrito</button>):(<a aria-label='WhatsApp' href={`https://wa.me/${phone}`} target='_blank' className='btn btn-primary shadow-soft'>WhatsApp</a>)}</div>)}
